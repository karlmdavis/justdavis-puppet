# The default node configuration.
# This will only be used for nodes that do not have a "real" matching node definition.
node default {
  # Warning: Don't put any configuration in here. Either put it in a specific node definition or in sites.pp.
  # This file is really just here because Puppet requires it for nodes that don't have specific configs.
}
